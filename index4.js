// stack vs heap 
//primitive data stored in stack and refrence data stored in heap
const name='John'
const age=30
const person=
{
    name:'Jonathan',
    age:31
}
const newName=name;
console.log(name,newName);